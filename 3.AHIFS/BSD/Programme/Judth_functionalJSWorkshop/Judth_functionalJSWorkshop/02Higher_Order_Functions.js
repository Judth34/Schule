function repeat(operation, num) {
    var f = function () { console.log("Hallo") }
    function higherOrderFunction(fu, num) {
        for (int = idx = 0; idx < num; idx++) {
            f();
        }
    }
}

// Do not remove the line below
module.exports = repeat